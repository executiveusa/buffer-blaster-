# Pinned to a mutually compatible set. The dependabot bump pulled starlette
# to 1.x, which broke fastapi 0.115 (Router no longer accepts on_startup).
# FastAPI 0.123+ supports starlette 1.x. Starlette is pinned explicitly so a
# future transitive bump can't silently break the test suite again.
fastapi==0.141.1
starlette>=1.6.0,<2.0
uvicorn[standard]==0.52.4
pydantic==2.13.5
supabase==2.31.0
httpx==0.28.1
python-multipart==0.0.32
python-dotenv==1.2.3
cryptography==50.0.1
redis==8.1.0
pytest==9.1.1
pytest-asyncio==1.4.0
