"""Stavarai Platform — FastAPI backend package.

The application instance lives in `api.app:app`. This file is intentionally
minimal so `import api` works for tooling without triggering router imports
(which require the full FastAPI dependency tree to be installed).
"""
__version__ = "1.0.0"
